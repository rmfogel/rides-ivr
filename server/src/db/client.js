// Deprecated Postgres client stub left intentionally empty after migration to MongoDB.
export function getPool() { return null; }
export async function query() { throw new Error('Postgres removed; use Mongo repo functions'); }
